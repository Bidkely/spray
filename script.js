// Animation JavaScript
        document.addEventListener('DOMContentLoaded', function() {
            // Trigger fade-in animation for main content after a short delay
            setTimeout(function() {
                document.querySelector('main').classList.add('animate');
            }, 300);
        });